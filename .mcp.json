{
  "mcpServers": {
    "launchpath": {
      "command": "npx",
      "args": ["-y", "@launchpath-ai/mcp-server"],
      "env": {
        "LAUNCHPATH_API_KEY": {
          "description": "Your LaunchPath API key. Get it from Settings → API Keys in the LaunchPath dashboard (https://www.trylaunchpath.com/dashboard/settings).",
          "required": true
        }
      }
    }
  }
}

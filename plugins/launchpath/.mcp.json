{
  "mcpServers": {
    "launchpath": {
      "command": "npx",
      "args": ["-y", "@launchpath-ai/mcp-server"],
      "env": {
        "LAUNCHPATH_API_KEY": "${LAUNCHPATH_API_KEY}"
      }
    }
  }
}
